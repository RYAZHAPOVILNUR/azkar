# Азкар — план дизайн-доработок (UI/UX аудит)

Аудит проведён по всем экранам вживую (azkar.nurtech.dev/app, свет/тёмная, мобильный вьюпорт).
Общая система крепкая (единая шапка, радиусы, шалфейный акцент, секции-кэпшены). Ниже — конкретные фиксы по приоритету. Отмечаем `[x]` по мере готовности.

## P1 — быстрые баг-фиксы (заметное за малую правку)
- [ ] Карта прогресса Корана: убрать висячий `_` (пустой подзаголовок) под кнопкой «Повторить заученное».
- [ ] Библиотека: чип «На сверке · N» обрезается за край экрана — починить прокрутку/перенос ряда фильтров.
- [ ] Коран, список сур: вёрстка ломается на длинных именах («Аль-Имран» переносится, перевод обрезается, скачет высота строки) → имя `nowrap` / перевод на своей строке, ровная высота.

## P2 — облегчить перегруженные экраны
- [ ] Коран, читалка: три кнопки (Тафсир/Заучил/Слушать) под каждым аятом → облегчить (иконки без подписей / одна тонкая строка / по тапу). Убрать неоднозначность «Заучил ✓» (состояние vs действие).
- [ ] Коран, список сур: причесать строку целиком (номер-ромб, имя, перевод, мета, арабский) под единый ритм.

## P3 — пустые состояния (по образцу главной)
- [ ] Кибла: при нет-доступа/нет-локации — одна кнопка «Разрешить геолокацию» вместо трёх плиток с прочерками.
- [ ] Карта прогресса / прогресс Корана: приглашение вместо мёртвых нулей.

## P4 — Библиотека азкаров (крупный заход, старый батч)
- [ ] Карточки-разделы компактнее (сейчас ~300px картинка, влезает 2–3).
- [ ] Убрать мод-статусы для пользователя («Проверено/На сверке») и жаргон «из текущей проверенной базы» → нейтральные подписи.
- [ ] Группировка карточками внутри категории (было в старом батче: контраст тем, высоты блоков, двойные скобки, шеринг большого аскара).

## P5 — сквозная чистка копирайта
- [ ] Убрать внутренний жаргон в текстах для пользователя («проверенная база», «На сверке» и т.п.).

## Готово вне этого плана (контекст)
- Главная: Apple-дашборд «Сегодня» (кольцо/бары/секции/пустое состояние/шалфейный акцент).
- Карточка аската: упрощена (тихий номер, действия вправо, «проверено» скрыто, источник строкой).
- Кибла: редизайн циферблата + непрерывный хаптик + иконка мечети.
- Тёмный таджвид-мусхаф: font-palette base-palette:1.
- Аудио аскаров: прокси /api/azkar-audio + кнопка на карточке.

---
Правки — в `miniapp/index.html`. Деплой: scp + `docker compose up -d --build --force-recreate app` на 45.77.66.108. Q_ASSET_V бампать при правке JSON Корана. Осторожно с параллельным Codex — `git pull --rebase` перед коммитом.
