# Азкар — план дизайн-доработок (UI/UX аудит)

Аудит проведён по всем экранам вживую (azkar.nurtech.dev/app, свет/тёмная, мобильный вьюпорт).
Общая система крепкая (единая шапка, радиусы, шалфейный акцент, секции-кэпшены). Ниже — конкретные фиксы по приоритету. Отмечаем `[x]` по мере готовности.

## P1 — быстрые баг-фиксы (заметное за малую правку) ✅ ГОТОВО (cd609e3)
- [x] Карта прогресса Корана: убран висячий `_` + сброс утечки состояния кнопки повторения.
- [x] Библиотека: фейд-намёк на прокрутку ряда фильтров (состав чипов «Проверено/На сверке» — в P5).
- [x] Коран, список сур: имя не ломается по дефису (nowrap, приоритет над переводом), ровная высота.
- [x] БОНУС: «Продолжить чтение» — подпись не липнет к заголовку (block+ellipsis).

## P2 — облегчить перегруженные экраны ✅ ГОТОВО (cd609e3)
- [x] Коран, читалка: три кнопки → тихая ghost-строка (без рамки/фона, приглушённые, акцент активному).
- [x] Мем-кнопка: незаучено = плюс + «Заучить», заучено = галочка + «Заучено» (убрана неоднозначность).

## P3 — пустые состояния (по образцу главной)
- [x] Кибла: CTA «Разрешить геолокацию» + приглушённый циферблат вместо трёх «—» (7c12edd).
- [~] Карта прогресса / прогресс Корана: приемлемо (0% честный, не прочерки; кнопка повторения уже прячется в P1). Приглашение — при желании позже.

## P4 — Библиотека азкаров (крупный заход, старый батч)
- [x] Двойные скобки: чистка (( )) и одиночной ( ) обёртки корпуса + хвостовых [ ] заметок (cleanAzkarArabic на источнике; ﴿ ﴾ сохранены). 09f901e/f4f0dce.
- [x] Мод-статусы убраны: чипы «Проверено/На сверке» → «Все» + полезный «С аудио» (плоский список). Статус-жаргон убран из карточек результатов.
- [~] Шеринг большого аскара: правки (растущий холст + читаемый пол шрифта) ОТКАЧЕНЫ — превью нестабильно в headless-браузере, не смог надёжно проверить. НУЖЕН ТЕСТ НА РЕАЛЬНОМ УСТРОЙСТВЕ, потом переделать. Рабочая версия шеринга сохранена (checkout f4f0dce).
- [x] «Куча с перегородками» внутри категории: реальная причина — ~1 группа на аскар (daily 213/125, travel 10/9), разделитель рисовался перед каждым. Фикс: .zikr-section только для групп ≥2 аскаров. «Путешествие»: 9 перегородок → 1. Карточки групп НЕ нужны (дублировали бы список). b9b1aa3.
- [ ] Карточки-разделы компактнее (сейчас 162px, 1 колонка @≤380px, 14 категорий → длинный скролл). Не критично.
- [ ] Контраст тем в библиотеке: `.azlib-result` уже на тема-переменных; проверить визуально в тёмной, есть ли реальная проблема.

## Репорты пользователя, батч 2 ✅ ГОТОВО (bf0b95a)
- [x] Клавиатура поиска библиотеки не закрывалась → блюр инпута при листании.
- [x] Все чтецы (42, everyayah как quran.nurtech.dev) + лист выбора из мини-плеера (тап по подписи) и из настроек.
- [x] «Рандомные цифры» в категориях = бейдж карточки брал z.n из чужой сессии → теперь порядковый idx+1 (проверено: 1,2,3…).
- [x] Формат отсчёта намаза «через 02:14» (читалось мин:сек) → «через N ч M мин».
- [x] Радио переработано: «Сейчас играет» (live-пульс, статус, Пауза/Продолжить, Стоп); станции переключаются БЕЗ закрытия шторки; чистые названия; пауза/возобновление.

## Баги (репорты пользователя, вне плана) ✅ ГОТОВО (0585434)
- [x] «0 аятов» на главной после чтения / долгое обновление: showScreen при входе на home зовёт updateHomeProgress; уход с читалки flush-ит время (qClockStop). Проверено: 12→5 мгновенно.
- [x] «Левые буквы» первую секунду при открытии суры: QCF font-display swap→block (PUA-глифы не мигают фолбэком). Проверено: QCFT1 = block.

## P5 — сквозная чистка копирайта
- [x] Жаргон «из текущей проверенной базы» вычищен, «Запланировано к сверке» → «Скоро добавим», статус убран из карточек.

## Готово вне этого плана (контекст)
- Главная: Apple-дашборд «Сегодня» (кольцо/бары/секции/пустое состояние/шалфейный акцент).
- Карточка аската: упрощена (тихий номер, действия вправо, «проверено» скрыто, источник строкой).
- Кибла: редизайн циферблата + непрерывный хаптик + иконка мечети.
- Тёмный таджвид-мусхаф: font-palette base-palette:1.
- Аудио аскаров: прокси /api/azkar-audio + кнопка на карточке.

---
Правки — в `miniapp/index.html`. Деплой: scp + `docker compose up -d --build --force-recreate app` на 45.77.66.108. Q_ASSET_V бампать при правке JSON Корана. Осторожно с параллельным Codex — `git pull --rebase` перед коммитом.
